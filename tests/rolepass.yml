---
- hosts: localhost
  remote_user: root

  roles:

    - {role: testForFolder, path: "/etc", state: present, debug: false }
    - {role: testForFolder, path: "/etcbadfolder", state: absent, debug: false }

