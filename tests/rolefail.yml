---
- hosts: localhost
  remote_user: root

  roles:

    - {role: testForFolder, path: "/etc", state: absent, debug: false }
    - {role: testForFolder, path: "/etcbadfolder", state: present, debug: false }
